// src/modules/world/store/worldBook.store.ts
import { defineStore } from 'pinia';
// import type { WorldBookEntry } from '@world/types/worldBook.types'; // 路径待确认和创建类型文件

export const useWorldBookStore = defineStore('worldBook', () => {
  // --- State ---
  // const entries = ref<WorldBookEntry[]>([]); // 示例，后续实现
  // const currentWorld = ref<string | null>(null);

  // --- Getters ---
  // ...

  // --- Actions ---
  // function addEntry(entry: WorldBookEntry) {
  //   // ...
  // }

  // function removeEntry(entryId: string) {
  //   // ...
  // }

  // function loadWorldBook(worldName: string) {
  //   // ...
  // }

  return {
    // entries,
    // currentWorld,
    // addEntry,
    // removeEntry,
    // loadWorldBook,
  };
});